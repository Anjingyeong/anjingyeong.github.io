import mermaid from 'mermaid';
// Can't easily test mermaid server-side without headless browser.
// I will just use run_command with a simple node script that compiles it, or I can just fix the syntax.
